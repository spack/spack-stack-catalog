spack:
  specs:
    - alien+hypre+petsc~xml~hdf5+move+ref
    # ~hwloc due to openmpi 3 conflicts with hwloc2
    - arcane~wrapper~monoembed~hwloc
    - googletest  # Build dependency
  concretization: together

