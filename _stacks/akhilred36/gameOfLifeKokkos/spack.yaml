# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs: [cuda%gcc@10.2.0, valgrind, openmpi+cuda, kokkos@3.5.00+cuda+cuda_lambda+wrapper,
    kokkos@3.5.00+cuda+cuda_lambda+wrapper cuda_arch=35, gcc@10.2.0]
  view: true
